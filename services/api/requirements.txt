# ---------------------------------------------------------------------------
# METRASIGHT — API service runtime dependencies
# Target: Python 3.11+
# ---------------------------------------------------------------------------
fastapi>=0.115,<0.117
uvicorn[standard]>=0.30
pydantic>=2.7,<3
pydantic-settings>=2.3
SQLAlchemy>=2.0.30,<2.1
alembic>=1.13

# PostgreSQL driver (production). Not required for the SQLite dev/test path.
psycopg2-binary>=2.9

# Auth & security
PyJWT>=2.8
bcrypt>=4.1

# Uploads & structured logging
python-multipart>=0.0.9
structlog>=24.1

# Image intake (Prompt 3): deterministic decoding, EXIF orientation, quality
# statistics and safe resizing. No numpy/opencv — Pillow only.
Pillow>=10,<12

# UI-08 — official report exports: structured PDF via reportlab and editable
# DOCX via python-docx (both rendered server-side from real inspection data).
reportlab>=4,<5
python-docx>=1.1,<2

# Real perception (Prompt 4): local CPU OCR via PaddleOCR.
# Version pins matter: paddleocr 3.1.0 requires paddlex==3.1.x (3.7+ changes
# PaddlePredictorOption.__init__ and breaks engine construction). paddlepaddle
# is the CPU inference runtime; opencv-contrib-python and numpy are used both
# by paddleocr and directly (QR/barcode vision service, image decode).
paddlepaddle==3.0.0
paddleocr==3.1.0
paddlex==3.1.0
# paddleocr pulls paddlex[ie,...], which requires langchain>=0.2 with no
# upper bound. langchain 1.x removed the deprecated `langchain.docstore`
# module that paddlex 3.1.0 imports (via its retriever component), which
# breaks `from paddleocr import PaddleOCR` entirely — every OCR run fails
# with AI_SERVICE_UNAVAILABLE ("No module named 'langchain.docstore'",
# observed on a fresh 2026 Docker build). Pin to the 0.3.x line verified
# working with the paddle stack above (shim still present in 0.3.30).
langchain==0.3.30
langchain-community==0.3.31
langchain-core==0.3.86
opencv-contrib-python>=4.8,<5
numpy>=1.26,<3
